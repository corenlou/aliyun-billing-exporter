name: Build Docker Image

on:
  push:
    tags:
      # 触发条件：当推送标签符合 vX.X.X 格式时
      - 'v*.*.*'
  workflow_dispatch:      
      
env:
  # 推荐将仓库名设为 ENV 变量，方便复用
  IMAGE_NAME: aliyun-billing-exporter
  DOCKER_USER: devocenter # 🚨 替换成你的 Docker Hub 用户名/组织名
  
permissions:
  id-token: write      # Required for OIDC/JWT (虽然本例未使用，但保留)
  contents: read       # Required for actions/checkout

jobs:
  CI:
    name: Build
    runs-on: ubuntu-latest
    
    # outputs: # 此 Job 的输出 image_tag 仅用于 Job 间通信，本例中无需下一个 Job，可以移除
    #   image_tag: ${{ steps.tag.outputs.image_tag }} 
      
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      # 1. 登录 Docker Hub (docker.io)
      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          # 使用 secrets 传递用户名
          username: ${{ secrets.DOCKER_HUB_USERNAME }} 
          # 使用 secrets 传递 PAT (Personal Access Token)
          password: ${{ secrets.HUB_TOKEN }} 

      # 2. 设置镜像标签
      - name: Set image tag
        id: tag
        shell: bash
        run: |
          # $GITHUB_REF_NAME 包含了当前的标签名 (如 v1.0.0)
          echo "image_tag=${GITHUB_REF_NAME}" >> $GITHUB_OUTPUT 
      # 3. 设置 QEMU 和 Buildx (支持多架构构建)
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
        
      # 4. 构建并推送镜像
      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          # 🚨 完整的推送标签：用户/仓库名:标签
          tags: |
            ${{ env.DOCKER_USER }}/${{ env.IMAGE_NAME }}:${{ steps.tag.outputs.image_tag }}
            ${{ env.DOCKER_USER }}/${{ env.IMAGE_NAME }}:latest
